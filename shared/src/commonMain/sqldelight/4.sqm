-- 4.sqm: version 4 -> 5. Public groups (feature.publicGroups): a group is
-- 'private' (invite only) or 'public' (listed, anybody may join).
ALTER TABLE Groups ADD COLUMN visibility TEXT NOT NULL DEFAULT 'private';
