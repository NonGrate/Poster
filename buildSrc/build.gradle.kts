// Build logic shared by the module build scripts: the palette derivation that
// turns two seed colours in poster.properties into every Material role.
plugins { `kotlin-dsl` }
repositories { mavenCentral() }
