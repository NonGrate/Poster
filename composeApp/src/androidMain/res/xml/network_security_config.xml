<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext off everywhere, because the app carries a bearer token on every
  request and the platform is the only thing that refuses an http:// URL
  whatever the code does. android:usesCleartextTraffic="true" used to switch
  that guard off for every build, including release.

  The two exceptions are the machine running a local server: 10.0.2.2 is the
  host as the emulator sees it, and localhost is a phone on adb reverse. Both
  are debug conveniences and neither exists on a real network.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
