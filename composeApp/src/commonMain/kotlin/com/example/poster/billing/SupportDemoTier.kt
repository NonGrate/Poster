package com.example.poster.billing

/**
 * A placeholder support tier for the demo paywall, used only for screenshots
 * taken before a store account and RevenueCat offering exist.
 *
 * [id] matches a real tier id so the paywall shows its localized name; [price]
 * is an illustrative string, not a real store price.
 */
data class SupportDemoTier(val id: String, val price: String)
